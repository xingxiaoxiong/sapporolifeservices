<div class ="jumbotron">
  <h2>给出您的评论，给别人以借鉴</h2>
  <h3>Explore the favorite diners, pastry shops, and eateries in your community.
  <br>Leave reviews and ratings for your fellow foodies.</h3>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Address</th>
      <th>Rent</th>
      <th>Link</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @houses.each do |house| %>
      <tr>
        <td><%= image_tag house.image.url(:medium) %></td>
        <td><%= house.name %></td>
        <td><%= house.address %></td>
        <td><%= house.rent %></td>
        <td><%= house.link %></td>
        <td><%= link_to 'Show', house, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_house_path(house), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', house, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New House', new_house_path %>
